import chai from 'chai';
import chaiDom from 'chai-dom';

chai.use(chaiDom);
const { expect } = chai;

describe('Rxjs', function () {
    let rxjs = document.createElement('div');

    describe('items focus state', function () {
        console.log(rxjs);
    });

});
